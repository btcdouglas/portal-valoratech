import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel gerencia Next.js nativamente
  // Server Actions, API Routes e Image Optimization disponíveis quando necessário
};

export default nextConfig;
